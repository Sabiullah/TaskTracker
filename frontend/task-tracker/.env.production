# Backend for the Android APK build. The server 49.12.190.43 force-redirects
# HTTP to HTTPS and its certificate is issued for this domain, so the domain
# (which resolves to 49.12.190.43) must be used instead of the raw IP.
VITE_API_BASE_URL=https://tasktracker.fourdadvisory.com/api
VITE_WS_URL=wss://tasktracker.fourdadvisory.com/ws/
